---
name: data-search-test
description: 数据搜索一键测试。两种用法：①用户以「测：」开头提问→回答后自动核查+记录；②用户@本技能说「记录一下」→对上一轮已回答的问题做核查+记录。记录内容(问题/耗时/调用API/支持情况/正确性/备注)写入 Excel。
---

# 数据搜索一键测试

核心目标：用户发来一条投研问题，你**正常回答**，然后**在同一轮自动把本次测试的元数据追加到 Excel**。用户不需要再发第二条消息。

Excel 文件：
```
C:\Users\ziyao.yang.int\Desktop\yzy-0622\数据搜索\robowork测试记录.xlsx
```

## 两种入口，两种流程

本技能有两种触发方式，流程不同：

### 入口 A：用户以「测：」开头发起新问题

用户一次性完成提问+记录，例如：

> 测：比亚迪最新估值是多少？

完整流程：**阶段 1（回答）→ 阶段 2（核查）→ 阶段 3（写 Excel）→ 阶段 4（摘要）**。

### 入口 B：用户 @ 本技能说「记录一下」

用户在**之前的对话中已经提过问题并得到了回答**，现在只想把那次的结果记录下来。你说的「记录一下」意味着：**跳过阶段 1，直接进入阶段 2→3→4**——对上一轮回答中的关键数字做交叉核查，然后写入 Excel。

用户可能附带耗时信息，如「记录一下 时间2m36s」；没说明则耗时留空。

---

## 完整流程（入口 A 走全部 4 阶段，入口 B 从阶段 2 开始）

### 阶段 1：像平常一样回答用户问题（仅入口 A）

用户以「测：」开头提了一个投研问题，你**先正常调用 data_search / ai_search / macro_industry_data 等工具获取数据并回答**，跟平时完全一样。回答中出现的所有数字句必须溯源引用。

### 阶段 2：数据核查（入口 A 和 B 都执行）

回答完问题后，**先复盘阶段 1 调了什么工具**，再根据数据来源类型分两种策略核查：

**步骤 2.1：复盘阶段 1 的调用清单**

在开始核查前，先梳理本轮实际调用了哪些工具、每条关键数字的来源类型：

| 原始数据来源 | 含义 | 核查策略 |
|-------------|------|---------|
| `data_search` 结构化接口 | 通过 `call_api` 从 Datayes 接口直接取的数值 | **重调同一接口**复核 |
| `macro_industry_data` 指标库 | 从通联宏观/行业/特色指标库中取的时序数据 | **重调同一接口**复核 |
| `ai_search` 语义检索 | 从研报/新闻/公告语料库中语义搜索匹配到的数值 | **换源交叉验证** |

**步骤 2.2：按来源选择核查方式**

数据来源分两类，核查方式不同——核心原则：**结构化接口的数据是确定性的，重调即可验证；语义检索的结果不稳定，必须换源交叉验证**。

| 原始数据来源 | 核查方式 | 说明 |
|-------------|---------|------|
| 来自 `data_search` 或 `macro_industry_data` 结构化接口 | → 🔵 **重调同一接口**，用相同参数再取一次，对比两次返回是否一致 | 结构化接口返回是确定的，重调就是最直接的复核；不需要换源（换到 ai_search 反而可能因为语料时滞导致假偏差） |
| 来自 `ai_search` 语义检索 | → 🔴 **必须换源交叉验证**：优先尝试 `data_search` 结构化接口取数（如有对应接口）；无对应结构化接口则用不同关键词／不同时间窗的 `ai_search` 再搜一次做交叉确认 | 语义检索结果受 query、时间窗、语料更新等因素影响，必须正交验证 |

**步骤 2.3：构造核查 query 并执行**

- 每个待核查指标构造 **1~2 个**正交 query
- 并行执行所有核查 query
- 对比报告值与核查值，给出核查结论：✅ 一致 / ⚠️ 偏差 / ❌ 不符 / ❓ 未查到

**步骤 2.4：输出核查汇总表**

核查报告精简为一张表，直接附在回复中：

```
| # | 指标 | 主体 | 报告值 | 核查值 | 核查来源 | 状态 |
|---|------|------|--------|--------|---------|------|
```

不需要完整的 datayes-data-verification 5 阶段报告格式，关键是把交叉验证的结论说清楚。

### 阶段 3：写入 Excel

核查报告产出后，整理本次测试的元数据写入 Excel。

**字段填写规则**：

| 字段 | 填写规则 |
|------|----------|
| **问题** | 用户原始提问内容（去掉"测："前缀） |
| **查询耗时** | 用户自然语言告知（如"大概3分钟""2m30s"）；没提则留空 |
| **查询结果** | 固定留空 |
| **结果是否正确** | 基于阶段 2 核查结论填写：全部 ✅ → "正确"；有 ⚠️/❌ → "部分偏差"；全部 ❓ → "未查到" |
| **调用API** | 🔴 只列阶段 1 用于**获取原始数据**的接口（不含阶段 2 核查用接口）；格式：`data_search接口名` 或 `ai_search(主题)` 或 `macro_industry_data(指标)`，分号分隔 |
| **API数据是否支持** | 🔴 核心字段，给算法团队看——判断依据：**有没有结构化接口能直接返回这批数据** |
| **备注** | 🔴 给算法团队看——若 API 不支持，必须写清楚「缺什么接口、为什么只能用 ai_search 兜底、建议补什么」。无问题则留空 |

**「API数据是否支持」详细判定标准**：

| 值 | 含义 | 示例场景 |
|----|------|---------|
| **支持** | data_search / macro_industry_data 的结构化接口直接返回了所需的全部字段 | `getMktEqudEval` 返回 PE/PB，`fdmtNew` 返回营收利润 |
| **部分支持** | 结构化接口返回了部分字段，但某些维度缺失，需要 ai_search 辅助 | 接口有回购公告列表但无反查实际回购金额的字段 |
| **不支持** | 完全靠 ai_search 从研报/新闻/公告里语义检索兜底，没有对应结构化接口 | "股价低位回购公司"这类复合筛选条件，无现成接口 |

**「备注」填写要求**（给算法团队优化用）：

🔴 **当 `API数据是否支持` 判定为"不支持"或"部分支持"时，备注为必填且必须按以下模板写**，不得省略任何标签：

```
【缺什么】<缺失的具体数据维度，如"无龙虎榜每日机构席位买卖排行接口">
【现状】<当前如何拿到数据，如"ai_search 2次语义检索，从国信证券金融工程日报/晨会纪要中提取机构净买卖排名，仅能获取股票名单、无具体净买卖金额">
【建议】<后续可补充什么接口/字段，如"建议补充龙虎榜席位交易明细接口（含日期/股票代码/席位类型/买卖金额/净额），支持按机构专用/营业部/北向资金筛">
```

要求：
- 三标签**【缺什么】【现状】【建议】**缺一不可，每条用方括号标签开头。
- 内容具体到接口名/字段名/搜索主题，不要笼统写"接口不完善"之类空话。
- 支持=**支持**时备注留空，无需填模板。
- 接口有报错/workaround/缺字段/数据不全的额外信息，追加在【建议】之后。

**调脚本写入**：

```bash
python "C:\Users\ziyao.yang.int\AppData\Roaming\@robowork\shell\user-skills\data-search-test\scripts\write_test_record.py" \
  --question "用户提问" \
  --time "用户说的耗时" \
  --correct "核查结论：正确/部分偏差/未查到" \
  --api "实际调用的接口列表" \
  --supported "支持/部分支持/不支持" \
  --notes "给算法团队的优化建议（含缺什么-现状-建议三要素）"
```

### 阶段 4：输出摘要行

在回复末尾附上：

```
📋 已记录 | 耗时 XmXs | API: xxx | 支持=是/否/部分 | 结果=正确/部分偏差/未查到
```

---

## 重要提醒

- **先回答 → 再核查 → 再写 Excel**——按序完成。
- **耗时由用户告知**——用户说耗时你照录；没提就留空，不要自己估算或写在备注。
- **API数据是否支持**和**备注**是给算法团队的——写清结构化接口的覆盖盲区。
- 🔴 **supported≠"支持"时备注强制必填**：只要 `--supported` 传的是「部分支持」或「不支持」，`--notes` 就必须包含【缺什么】【现状】【建议】三要素，一条都不能少。脚本已内置校验——缺备注会报错并拒绝写入。只有 `--supported "支持"` 才允许 `--notes` 留空。
- **核查策略分两类**——结构化接口（`data_search` / `macro_industry_data`）的数据是确定性的，**重调同一接口**即可复核，无需换源；语义检索（`ai_search`）的结果受多因素影响，**必须换源正交验证**：优先用 `data_search` 结构接口核查，无对应接口则换关键词/时间窗再搜。
- **API 列表只录阶段 1**——只写阶段 1 获取原始数据实际调用的接口，阶段 2 核查用的接口（如正交交叉验证的 ai_search）一律不记。
- 非投研问题（闲聊等）一样正常回答并记录，问题字段如实填写。
